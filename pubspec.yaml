name: transport_book_app
description: TMS Prime - Transport Management System
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  country_code_picker: ^3.0.0
  http: ^1.1.0
  intl: ^0.18.0
  image_picker: ^1.0.4
  url_launcher: ^6.2.1
  share_plus: ^7.2.1
  flutter_contacts: ^1.1.7+1
  pdf: ^3.10.7
  printing: ^5.11.1
  path_provider: ^2.1.1
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
